# IVHealthClinics — TODO for 2026-04-25

Picking up where we left off. Goal of next session: get `places_discovery` data merged into the main `clinics` table so the directory has its full ~3,300 clinic inventory ready for Phase 3.2 (website crawling).

---

## Pre-flight (do this first, ~2 minutes)

- [ ] Replace existing `CLAUDE2.md` with the merged version from today's outputs
- [ ] Verify `places_discovery` table still has all 3,296 rows in Supabase
  ```sql
  SELECT COUNT(*) FROM places_discovery;
  -- Expected: 3296
  ```

---

## Decision 1: Quality filter (~5 min)

Apply a quality threshold to `places_discovery` before merging. Recommend 5+ reviews as the floor — filters out ghost listings without being overly restrictive.

- [ ] Run quality preview query to confirm filter impact:
  ```sql
  SELECT 
    COUNT(*) FILTER (WHERE rating_count IS NULL OR rating_count = 0) AS would_drop_zero_reviews,
    COUNT(*) FILTER (WHERE rating_count >= 5) AS would_keep_5_plus,
    COUNT(*) FILTER (WHERE rating_count >= 10) AS would_keep_10_plus
  FROM places_discovery;
  ```
- [ ] Decide: 5+ reviews (recommended) vs 10+ reviews (stricter) vs no filter
- [ ] Document choice in CLAUDE2.md

---

## Decision 2: Geographic gap-fill (~10 min decision, ~10 min runtime if yes)

Optional second-pass on ~30 underrepresented suburban metros (NJ suburbs, Chicagoland, MD/DC, Long Island, etc.) for ~$5 to add 500-1000 more clinics.

- [ ] Decide: do gap-fill BEFORE merge, AFTER merge as growth lever, or never
- [ ] If yes: ask Claude to generate the gap-fill metro list and run

---

## Decision 3: Merge migration (~30-45 min, the main work)

Schema-extend `clinics` table to absorb `places_discovery` rows. This is the big one.

- [ ] Ask Claude to generate migration SQL with these steps:
  1. Add `source` column to `clinics` ('npi' | 'google_places' | 'manual')
  2. Backfill existing rows: `source = 'npi'`
  3. Make NPI-specific columns nullable if not already (npi, taxonomy_code, etc.)
  4. Insert from `places_discovery` into `clinics` with:
     - `source = 'google_places'`
     - `enrichment_status = 'enriched'`
     - `match_confidence = 'high'` (or tier based on rating_count)
     - `is_iv_clinic = true` (these passed Google search → presumed IV)
     - Copy: name, address, city, state, zip, phone, website, latitude, longitude, rating_value, rating_count, business_status, google_place_id
  5. Skip rows whose `google_place_id` already exists in `clinics` (the 16 overlap rows)
  6. Mark merged `places_discovery` rows: `merge_status = 'merged'`, `merged_into_clinic_id`, `reviewed_at = NOW()`

- [ ] Run migration in **dry-run mode first** (SELECT what would be inserted, count rows)
- [ ] Verify counts match expectations (~3,000 inserts depending on filter)
- [ ] Run actual migration
- [ ] Verify with sanity queries:
  ```sql
  SELECT source, COUNT(*) FROM clinics GROUP BY source;
  -- Expected: npi=216, google_places=~3,000
  
  SELECT match_confidence, COUNT(*) FROM clinics 
  WHERE source = 'google_places' GROUP BY match_confidence;
  ```
- [ ] Spot-check 5 random merged rows on Google Maps — verify lat/lng accuracy

---

## Decision 4: Slug generation for new clinics (~15 min)

Discovery clinics don't have slugs yet — they need URL-safe slugs for `/clinics/[slug]` routing.

- [ ] Confirm slug generation function exists in `src/lib/` (used during NPI seeding)
- [ ] Generate slugs for all `source='google_places'` rows where `slug IS NULL`:
  - Format: `lowercase-name-city-state` (e.g., `manhattan-iv-therapy-new-york-ny`)
  - Handle collisions: append `-2`, `-3`, etc.
- [ ] Either run as SQL UPDATE with regex or write a small Python script

---

## Verification (~15 min)

After all migrations, verify the directory works end-to-end:

- [ ] Run dev server: `npm run dev`
- [ ] Browse `/clinics` listing page — should show new mix of clinics
- [ ] Visit `/clinics/[slug]` for 3-5 newly merged clinics — verify pages render
- [ ] Check `/sitemap.xml` includes new clinics (should now have ~3,400 URLs)
- [ ] Visit a state page (e.g., `/states/florida`) — verify higher clinic count
- [ ] Visit a city page for a major metro — verify proper clinics listed

---

## Deploy & Commit (~10 min)

- [ ] `npm run build` locally — fix any TypeScript errors from new columns
- [ ] Add `as any` casts where needed for new columns until types regenerate
- [ ] `git add` + commit migrations + script changes + CLAUDE2.md update
- [ ] `git push` — Vercel auto-deploys
- [ ] Verify production site loads correctly
- [ ] Check Google Search Console for sitemap re-submission (or wait for daily crawl)

---

## After Merge — Plan Phase 3.2

Don't start coding Phase 3.2 today, just plan it:

- [ ] Review HormoneMap's Crawl4AI + Claude Haiku setup (sister project pattern)
- [ ] Estimate budget: ~3,200 clinics × $0.05-0.10 per crawl = $150-300
- [ ] Decide priorities for fields to extract:
  - Service types (hydration, NAD+, Myers, vitamin shots)
  - Pricing (membership, drip menu, single visit)
  - Care setting (in-clinic / mobile / both)
  - Supervision level
  - Sterile compounding
  - Mobile service areas
- [ ] Decide order of operations: crawl all 3,200 vs crawl high-confidence first vs prioritize by metro

---

## Stretch Goals (only if time permits)

- [ ] Build a simple admin SQL view to spot-check merged clinics
- [ ] Generate a quick "directory health" dashboard query (clinics per state, avg rating, % with photos, etc.)
- [ ] Write the favicon / PWA icons that were left from Phase 2

---

## Time estimate

- Pre-flight + decisions: 15 min
- Quality filter + gap-fill decision: 15 min
- Merge migration (dry-run + actual): 45 min
- Slug generation: 15 min
- Verification: 15 min
- Deploy + commit: 10 min
- Phase 3.2 planning: 15 min

**Total: ~2-2.5 hours.** Solid one-session goal.

---

## What NOT to do tomorrow

- ❌ Don't start Phase 3.2 crawling yet — finish the merge first
- ❌ Don't optimize the discovery script further — it served its purpose
- ❌ Don't try to clean up the 22 `address_only` rows from NPI enrichment — Phase 3.2 will handle those
- ❌ Don't add more Places API discovery runs unless gap-fill is explicitly desired
